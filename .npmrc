# React Native / Expo require a flat (hoisted) node_modules layout so native
# module autolinking can resolve packages. pnpm defaults to a symlinked store,
# so we opt into the hoisted linker here.
node-linker=hoisted
